# Paso para el flujo básico de git
```sh
git status
git add . o git add -a
git status
git commit -m "Descripción del cambio"
git push
```
# Nota:
El mensaje del commit debe de ser una oración descriptiva del cambio